name: OpenAI Code Review
description: Composite action to perform OpenAI code review
inputs:
  diff-content:
    description: "Content of code diff"
    required: true
runs:
  using: "composite"
  steps:
    - name: Run OpenAI Code Review Script
      shell: bash
      run: node review-code/index.js
